#include <iostream>

using namespace std;

int main()
{
    cout << "Implment a stack where once an internal limit is exceeed, the stack creates a "
         << "new stack to hold all subsequent data. (literal stack of plates)" << endl;
    return 0;
}